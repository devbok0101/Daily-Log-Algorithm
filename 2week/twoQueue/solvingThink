## 접근 방법 생각하기

https://school.programmers.co.kr/learn/courses/30/lessons/118667

1. 두개의 큐가 있고
2. pop(), insert() 가 1회
3. 그래서 값이 같아지게 하기



1. 큐의 값을 모두 더해서, 2로 나눔 = 큐 한개당 나와야하는 값
2. 하나의 큐가 합이 클경우만 넘겨주기
3. 한바퀴를 돌면 즉 원소의 갯수만큼 반복되면 답이 없는 것으로 -1

1. deque()를 쓰거나 투포인터를 쓰거나
2. 매번 sum()하면 시간초과함, 제약조건 확인필 10의 8제곱 이상이면 O(n2) 로풀면 안됨


1. 원형 큐, 순환 인덱싱
```
(right + 1) % len(queue)

```